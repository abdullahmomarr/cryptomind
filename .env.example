# Copy to ".env" (or just export these in your shell) before running the live loop.
#
# Only needed when the AGENT uses the Claude LLM (the live loop, or `seed --engine llm`).
# The rule-based engine and all memory/data code run WITHOUT any key.

ANTHROPIC_API_KEY=sk-ant-your-key-here
